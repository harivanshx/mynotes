
``` python
cat_cols = df.select_dtypes(include = 'object').column
cat_cols 

# one hot encoding 

```


- In case of feature scaling data standerdization using z_score normalization can be done to transform the numaric values . 
- Data Transformation also  helps us to scale the features values.
- 


# Data Visualization

