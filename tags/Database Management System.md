
and 


## [[Database]]

- A database is a collection of data but in a organization way
- If we have data but not organized it is not a database it is just data storage
- Database is a organized collection of data
- Mostly we use rows and columns ie tables
- Sometimes we use the words attribute and columns

## DBMS
- We need to create our database and we need to create our tables
- Now we are working with database we need to take overview with
- the database and to perform operation on database we require some environment so that software program is known as dbms

### [[DDL]] and [[DML]] 
- DDL is the Data definition language
- DML is data manipulation laguage

### Transaction 
- a [[transaction]] is a series of operation perform to complete some task.
- we can control it by [[commit]] and [[rollback]]
- [[commit ]] is used to save changes completely and permanently in your database
- the moment you close your changes or save them it will commit so the data base server side we just use the word commit for that 
- if we want control in it that you want to reach to some previous state of the data initial state so for that we use roll back 
- [[rollback]] it will undo the part or complete transaction 
- we have these different type of statements
- [[DCL]] and [[DTL]]


#### [[Datatype]] in DBMS-- MYSql
int 
float
decimal
data
char
varchar
text


### Install MySql Server 

To install my sql server in your windows machine what you need to do is 




### Primary key and foreign key
In [[mysql]] in mysql we use primary key and 